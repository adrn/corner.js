/*
*/

var corner = {"__version__": "0.1"};

if (typeof exports !== "undefined") {
    if (typeof module !== "undefined" && module.exports)
        exports = module.exports = corner;
    exports.corner = corner;
} else {
    this.corner = corner;
}

corner.plot = function() {
    // derp

}
